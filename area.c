#include <stdio.h>

int main (){
 float Area , Radius;
 printf ("Enter the radius of circle to find the Area of circle : ");
 scanf ("%f", &Radius);

 Area = Radius * Radius * 3.14 ;
 printf ("the Area of the circle is : %f", Area );
 return 0;
  }
