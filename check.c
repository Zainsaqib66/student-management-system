#include <stdio.h>
int main (){
int a , b , c , max;
Printf("Enter the first number");
scanf("%d", &a);
Printf("Enter the second number");
scanf("%d", &b);
Printf("Enter the third number");
scanf("%d", &c);
if(a>=b && a>=c){
    max = a;
}else if (b>=a && b>=c){
    = b
